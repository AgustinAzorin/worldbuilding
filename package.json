{
  "name": "worldbuilding",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @worldbuilding/api run start:dev",
    "dev:web": "pnpm --filter @worldbuilding/web run dev",
    "build:api": "pnpm --filter @worldbuilding/api run build",
    "build:web": "pnpm --filter @worldbuilding/web run build"
  },
  "engines": {
    "pnpm": ">=9",
    "node": ">=20"
  }
}
